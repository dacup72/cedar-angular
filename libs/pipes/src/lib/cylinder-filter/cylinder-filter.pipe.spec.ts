import { CylinderFilterPipe } from './cylinder-filter.pipe';

describe('CylinderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CylinderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
