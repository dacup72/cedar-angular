import { GasProfileFilterPipe } from './gas-profile-filter.pipe';

describe('GasProfileFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GasProfileFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
